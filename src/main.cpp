#include "core.h"

// TODO: improve consistency w/ how some things are initialized
// ex. some just take parameters in their init() function
// and others require you to set properties and call init() w/ no params

int main() {
    core->run();
}
